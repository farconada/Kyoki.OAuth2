-
  name: 'authorization page'
  uriPattern:    'authorize'
  defaults:
        '@package':    'Kyoki.OAuth2'
        '@controller': 'OAuth'
        '@format':     'html'
        '@action':     'authorize'
